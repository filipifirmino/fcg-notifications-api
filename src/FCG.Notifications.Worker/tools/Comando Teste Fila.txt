# Publica os eventos de teste (UserCreated / PaymentProcessed) na fila do RabbitMQ.
# O TestPublisher e um utilitario independente e nao faz parte da solution (.sln) —
# rode-o diretamente via dotnet run. Caminho relativo a raiz do repositorio:

dotnet run --project src/FCG.Notifications.Worker/tools/FCG.Notifications.TestPublisher

# Opcional: informar o host do RabbitMQ (padrao = localhost)
# dotnet run --project src/FCG.Notifications.Worker/tools/FCG.Notifications.TestPublisher -- meu-host-rabbitmq
